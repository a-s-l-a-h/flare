# flareclient-extensions-builtin (web)

Official tasks (and, later, plugins) shipped with Flare, mirroring the
Android module. Each entry lives in its own folder. Depends on
flareclient only — flareclient never imports anything from here.