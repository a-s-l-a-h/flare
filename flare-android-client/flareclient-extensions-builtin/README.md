# flareclient-extensions-builtin

Official tasks and native panes shipped with Flare. Every entry has its
own folder with an implementation + README (id, params schema, platform
support, usage snippet). Depends on :flareclient only — never imported
back by :flareclient (see architectural note in the update spec).